# Spotify Clone (Static UI)

A lightweight Spotify-inspired landing page built with plain HTML and CSS.

## Run locally

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000` in your browser.
